defmodule Listeners do
  def run(_conn, _body_params) do
    # Find a way to call the correct listener according to the action property.
    # If possible, avoid to "register" the listeners manually ?
  end
end
